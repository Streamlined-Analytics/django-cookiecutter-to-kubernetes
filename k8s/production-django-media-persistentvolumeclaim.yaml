apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: production-django-media
  name: production-django-media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
