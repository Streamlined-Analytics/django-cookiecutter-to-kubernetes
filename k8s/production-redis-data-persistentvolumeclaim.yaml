apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: production-redis-data
  name: production-redis-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
